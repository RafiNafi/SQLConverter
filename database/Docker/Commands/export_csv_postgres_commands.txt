psql -U postgres -d northwind -c "COPY (SELECT * FROM products) TO STDOUT CSV" > products.csv

psql -U postgres -d northwind -c "COPY (SELECT * FROM orders) TO STDOUT CSV" > orders.csv

psql -U postgres -d northwind -c "COPY (SELECT * FROM order_details) TO STDOUT CSV" > order_details.csv

psql -U postgres -d northwind -c "COPY (SELECT * FROM categories) TO STDOUT CSV" > categories.csv

psql -U postgres -d northwind -c "COPY (SELECT * FROM employees) TO STDOUT CSV" > employees.csv

psql -U postgres -d northwind -c "COPY (SELECT * FROM suppliers) TO STDOUT CSV" > suppliers.csv